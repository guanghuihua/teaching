# 学生试卷排版规范

适用于本仓库中面向学生发放的练习、单元测验和试卷。答案版、教师讲义可以另行排版，但不得把提示性内容带入学生卷。

## 学生卷内容

- 删除“使用说明”、AI 生成痕迹、方法标签和“使用哪一个技巧”等提示。
- 题目保留来源、条件、设问和必要的图形依据；答案、解析、评分细则仅保留在答案版。
- 图形无法可靠核对时，宁可在题目中保留作图或作答位置，也不要使用未经验证的示意图。

## 留白和小题

- 选择题：不留演算空白。选项之后应紧接下一题，避免无意义的大片空白。
- 填空题：默认不留大块演算区；只在题干中提供必要的填写位置。需要推导的填空题，由教师明确要求后再增加空间。
- 主观题、解答题和证明题：在一道完整大题的最后一个设问之后预留作答区，默认高度为 `2.4cm`；题目计算量较大时可增加，但应按题目实际需要调整。
- 小题编号 `（1）`、`（2）`、`（i）`、`（ii）` 必须各自另起一行，编号和该小题正文在同一行；小题之间连续排版，不插入演算留白。
- 不让小题编号孤立在页末。必要时使用 `needspace` 将题号与至少两三行正文一同移到下一页。

## TeX 实现

内容文件使用 `exam-zh` 的 `question`、`choices`、`solution` 环境时，学生卷可通过 `choices` 环境区分选择题与主观题：

```tex
\usepackage{needspace}

\newcommand{\subquestion}[1]{%
  \par\Needspace{3\baselineskip}\noindent\textbf{#1}\quad
}
\newcommand{\subsubquestion}[1]{%
  \par\Needspace{3\baselineskip}\noindent\hspace*{2em}\textbf{#1}\quad
}
\newcommand{\studentworkarea}{\par\vspace*{2.4cm}}
\newif\ifquestionhaschoices
\BeforeBeginEnvironment{question}{\global\questionhaschoicesfalse}
\BeforeBeginEnvironment{choices}{\global\questionhaschoicestrue}
\BeforeBeginEnvironment{solution}{\ifquestionhaschoices\else\studentworkarea\fi}
```

这段实现依赖于每道选择题都使用 `choices` 环境。若题目类型采用其他环境，须在对应环境开始处设置同样的题型标记，不能重新使用“所有题目一律留白”的钩子。

## 交付前检查

1. 从 TeX 文件所在目录连续编译两次，并检查日志中没有 TeX error、未定义命令和 Overfull `\\hbox`。
2. 渲染并查看正式学生卷 PDF，而不是临时构建目录中的 PDF。
3. 逐页确认：选择题后无作答区；主观题后有留白；每个小题另起一行且编号与正文不分离；没有题号、文字、图形重叠或越界。
4. 只提交有意交付的 PDF 和其源文件；不提交 `.aux`、`.log`、`.out` 或临时渲染图片。
